# ============================================================
# DEPRECATED — 此文件已被替代
#
# 请将订阅 URL 切换至以下平台特定的 mihomo 配置:
#   Windows (Sparkle/Verge): clash/mihomo.sparkle.yaml
#   OpenWrt (nikki):         clash/mihomo.nikki.yaml
#   Android (CMFA/FlClash):  clash/mihomo.android.yaml
#
# 当前文件仅为兼容旧 raw URL 而保留,不可作为正常配置使用。
# ============================================================
proxies: []
proxy-groups:
  - name: "⚠️ 请更换订阅 URL — 见文件顶部注释"
    type: select
    proxies: [DIRECT]
rules:
  - MATCH,⚠️ 请更换订阅 URL — 见文件顶部注释
